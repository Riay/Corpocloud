<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.corpocloud.notes">  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.CorpoCloudNotes">

    <activity
        android:name=".MainActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity
        android:name=".ListNoteActivity"
        android:label="Mis Apuntes"
        android:parentActivityName=".MainActivity" />

    <activity
        android:name=".AddNoteActivity"
        android:label="Nueva Nota"
        android:parentActivityName=".ListNoteActivity" />

    <activity
        android:name=".ViewNoteActivity"
        android:label="Detalle del Apunte"
        android:parentActivityName=".ListNoteActivity" />

</application>
</manifest>